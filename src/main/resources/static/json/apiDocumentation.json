{
  "endpoints": [
    {
      "path": "/api/allRestaurants",
      "description": "Fetches all the restaurants in a paginated manner. It is not an option to fetch all the restaurants.",
      "queryParams": [
        {
          "query": "page",
          "type": "Integer",
          "defaultValue": 0,
          "required": false,
          "description": "Sets the page number of the pagination of the data"
        },
        {
          "query": "limit",
          "type": "Integer",
          "defaultValue": 10,
          "required": false,
          "description": "Sets the size of the pages in the pagination of the data"
        }
      ]
    },
    {
      "path": "/api/allTags",
      "description": "Fetches all the tags in a paginated manner.",
      "queryParams": [
        {
          "query": "page",
          "type": "Integer",
          "defaultValue": 0,
          "required": false,
          "description": "Sets the page number of the pagination of the data"
        },
        {
          "query": "limit",
          "type": "Integer",
          "defaultValue": 10,
          "required": false,
          "description": "Sets the size of the pages in the pagination of the data"
        }
      ]
    },
    {
      "path": "/api/restaurant",
      "description": "Fetches the info relating to a single restaurant.",
      "queryParams": [
        {
          "query": "id",
          "type": "String",
          "required": true,
          "description": "The id of the restaurant that is being fetched"
        }
      ]
    }
  ]
}